# 开发记录：架构基础开篇

- 日期：2026-08-23
- 里程碑：建立教材入口并完成第一章
- 完成后状态：已完成

## 结果

已在 `D:\Codes\software_design_guide` 建立独立的软件架构自学材料。课程主线确定为 Node.js + TypeScript，不再同时维护 Python 后端示例。

首批内容包含：

- 教材 README 和当前进度；
- 面向作者与协作 Agent 的写作规范；
- 六阶段学习路线；
- 第一章《软件架构首先管理变化》；
- 课程设计说明和首批实施计划。

## 内容决策

- 不从 SOLID 或 GoF 模式定义开始。
- 第一个贯穿案例使用任务管理 API，因为业务简单，能把注意力留给职责、依赖、测试和错误处理。
- 第一章只做一次最小函数边界提取，不提前引入 Repository、Controller、Service 或依赖注入容器。
- Markdown 是第一阶段唯一载体；Docusaurus、数据库和完整 Node.js 工程延后到对应问题真实出现时。
- 不按固定八周日历判断学习完成，而以复述、识别、设计和验证产物作为完成证据。

## 审查与修正

独立只读审查发现，第一版文章把教学代码片段写得像可以单独运行，却省略了 `database` 和 Node.js `crypto` 的来源。随后完成以下修正：

- 将开篇改为“现实中常见的处理函数”，不再称为完整可运行示例；
- 明确数据库和 HTTP 框架接线被省略；
- 显式使用 `node:crypto` 的 `randomUUID`；
- 将练习代码明确标记为用于分析职责的伪代码；
- 说明真实系统仍需处理任务不存在和外部调用失败。

## 验证

实际执行以下检查：

1. 递归列出全部交付文件并检查文件长度，未发现空文件。
2. 搜索第一章的“问题、术语、什么时候不需要、请用自己的话、练习”结构，五类内容均有命中。
3. 在用户可见正文中搜索 `FastAPI`、`Pydantic`、`pytest`、`Python 后端`，无匹配。
4. 使用区分大小写的未完成标记检查搜索 `TBD`、`TODO`、`待补充`，无匹配。
5. 对 README 相对链接执行存在性检查，全部目标存在。

首批内容没有可运行的 TypeScript 项目，因此没有执行类型检查、单元测试或构建。本章代码是明确标注依赖前提的教学片段。

## 已知限制

- 尚未实现任务管理 API。
- 尚未建立自动化 Markdown 链接检查脚本。
- Mermaid 图只做了源码结构检查，尚未通过渲染器验证。
- 练习答案尚未编写；为了避免提前暴露答案，将在读者完成练习后单独提供。

## 下一步

下一章讲解“职责、依赖方向、耦合与内聚”。它会继续使用任务创建案例，解释为什么简单地把函数搬到另一个文件不一定形成真正边界。

## Git

按照课程设计约束，没有初始化 Git，也没有创建提交。
