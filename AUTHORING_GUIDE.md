# 写作规范

这份文件约束本教材的作者和协作 Agent。目标不是让所有文章长得一样，而是防止教材重新变成术语堆积和答案背诵。

## 读者假设

默认读者能够阅读普通 TypeScript，但不假设读者已经理解：

- 耦合、内聚和依赖方向；
- SOLID 或设计模式；
- HTTP 框架、数据库和自动化测试；
- 企业项目中常见的分层名称；
- 一个抽象为什么值得存在。

不能使用“显而易见”“大家都知道”“正常项目都会”一类表达跳过推导。

## 每章的固定骨架

正文按下面的顺序组织：

```text
提出问题
→ 用短代码让问题落地
→ 先说人话
→ 在同一段引入术语
→ 做一个最小改进
→ 解释改进的代价
→ 说明什么时候不需要
→ 要求读者复述
→ 提供练习
→ 最后给图示和深入内容
```

如果某一章不适合代码，可以使用数据流、目录或操作步骤代替，但仍然必须先有问题，再有概念。

## 怎样引入专业词

错误写法：

> 应该使用 DIP，通过 IoC 和 DI 解耦基础设施。

这句话要求读者同时认识四个缩写，却没有告诉读者发生了什么问题。

推荐写法：

> 业务代码现在直接创建数据库客户端，因此更换数据库时，业务代码也被迫修改。模块之间这种互相牵连的程度叫作**耦合（coupling）**。我们先想办法让业务规则不再认识具体数据库，再讨论这种做法属于哪条原则。

专业词第一次出现时，应在同一段解释它描述的现象。英文名称用于建立检索能力，不用于代替中文解释。

## 代码规则

- 一个代码片段主要说明一个问题。
- 正文代码通常保持在 5 至 25 行。
- 省略内容时必须说明省略了什么，不能让示例看起来可以直接运行却实际缺少关键步骤。
- 错误示例必须“在当前规模下看起来合理”，不能故意写成没人会采用的笨代码。
- 改进示例只解决当前已经出现的问题。
- 不为了展示模式而创建类、接口、工厂或依赖注入容器。
- TypeScript 外部输入默认不可信；进入业务逻辑前必须说明由谁校验。

## 怎样避免过度设计

每次引入抽象前回答三个问题：

1. 当前哪一种变化正在制造修改成本？
2. 新边界把这种变化限制在哪里？
3. 新增的间接性是否比原问题更容易理解和维护？

如果第一问没有具体答案，就先不引入抽象。

每章必须包含“什么时候不需要”一节。设计方法是有成本的工具，不是越多越专业。

## 图示规则

图放在文字解释之后，用于第二次解释已经出现的关系。

适合画图的情况：

- 三个以上模块存在明确依赖方向；
- 一次请求经过多个阶段；
- 状态随着事件变化；
- 修改影响会沿依赖传播。

只有一个函数或两个对象时，优先使用短代码和普通文字。

## 理解检查

每章至少提供三类问题：

- **复述题**：不使用本章专业词，用自己的话解释问题。
- **识别题**：在新代码中找出相同现象。
- **取舍题**：判断什么时候保持简单反而更好。

答案不能只给结论，需要说明判断依据。

## 引用规则

引用公司工程文章或研究资料时采用：

1. 必要的短原文；
2. 中文直译；
3. 人话解释；
4. 对当前工程问题的影响；
5. 证据边界。

厂商经验、官方产品能力、观察性研究和因果实验必须区分，不能都写成已经证明的定律。
