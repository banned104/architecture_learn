# 开发记录：补全 26 章课后练习参考答案

- 日期：2026-08-24
- 范围：第 1—26 章的复述、识别、实现/设计、取舍和综合练习
- 状态：已完成

## 结果

保留原有《练习答案提示》作为快速自检版，新增《课后练习完整参考答案》作为逐题入口。完整答案覆盖：

- 26 个“请用自己的话解释”；
- 103 道练习与综合练习；
- 共 129 个逐题答案小节；
- 实现题所需的最小 TypeScript、SQL、表格或步骤；
- 每题的判断依据，以及开放题的其他可接受方案或常见误区。

README 和学习路线已经同时提供提示版与完整答案版入口，README 中的附录数量由五份更新为六份。

## 审查与修正

题目覆盖审查逐章比较第 1—26 章原文，确认 26 个解释题、103 道练习、原章链接和题目顺序均无遗漏或错配。

技术审查重点检查 TypeScript、状态迁移、测试契约、配置边界、数据库、幂等、部署、安全和第 26 章能力声明，并完成以下修正：

- 让第 1 章重复完成示例与当前 Task API 的幂等语义一致，并避免重复通知；
- 明确本项目沿用当前 `parsePort` 的 Number 转换契约，并把严格十进制保留为需同步修改实现与测试的备选方案；
- 补全或明确标注 TypeScript 结构片段的省略边界；
- 明确第 12 章状态集合是独立扩展示例；
- 将第 26 章五道综合题逐项标为目标架构，而非当前内存示例能力；
- 固定 `If-Match` 方案的版本冲突为 412，并说明应用层版本字段可选择 409；
- 明确当前 Repository 契约不保证列表顺序，若以后要求稳定排序需同时更新两个实现和共同测试。

## 验证

- `node scripts/verify-course.mjs`：通过；在创建本记录前报告 26 章、50 个 Markdown，TypeScript 门禁全部通过。
- `examples/task-api` 的 `npm run typecheck`：通过。
- `examples/task-api` 的 `npm test`：5 个测试文件、18 个测试通过。
- `examples/task-api` 的 `npm run build`：通过。

创建本记录并更新实施计划后的最终门禁结果见文末补记。

## 证据边界

- 完整答案不是所有开放题的唯一方案；关键是契约一致、判断依据明确并能验证。
- 第 26 章的数据库、幂等、Outbox、多租户、Webhook 和服务拆分仍是目标架构，不是当前示例已经实现的功能。

## Git

项目未初始化 Git，没有创建提交。

## 最终验证补记

创建本记录、更新实施计划并完成修正后，再次运行 `node scripts/verify-course.mjs`：通过；报告 26 章、51 个 Markdown，TypeScript 门禁全部通过。
